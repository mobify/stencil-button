{?href} <a href="{href}" {+attributes /}>
{:else} <button type="{?type}{type}{:else}button{/type}" {+attributes /}> {/href}

    <span class="c-button__inner {class}">{body}</span>

{?href} </a>
{:else} </button> {/href}

{<attributes}
    class="c-button {class}"
    {?id}id="{id}"{/id}
    {?role}role="{role}"{/role}
    {?onclick}onclick="{onclick}"{/onclick}
    {attr}
{/attributes}
